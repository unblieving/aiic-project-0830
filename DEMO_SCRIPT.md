# 3 分钟 Demo 视频脚本

## 0:00 - 0:30 开场和 Wow Moment

画面直接打开结果页或快速展示一次结果：

```text
TCP 三次握手
首次：L2
变式重测：L0 ✓
L2 -> L0 ✓
```

旁白：

> 这个项目不是又一个 ChatGPT 面试壳。它解决的是学生明明学过知识，但面试突然被问时调不出来的问题。核心证据是：刚才需要提示才能想起来的知识，经过训练后能在变式问题里无提示回答出来。

## 0:30 - 1:00 目标用户和取舍

展示首页：

> 目标用户是第一次准备大厂技术实习面试的本科生。他们不是完全不会，而是缺少主动输出和被追问时的提取训练。所以我刻意没做登录、分数、视频、排行榜，也没有做大而全题库，先把一个最小训练闭环做稳定。

## 1:00 - 2:15 核心流程演示

操作：

1. 选择“后端开发”。
2. 选择“计算机网络”和“操作系统”。
3. 给计算机网络选“高”，操作系统选“中”。
4. 点击“开始训练”。
5. 遇到 TCP 三次握手问题后，点击“我卡住了”。
6. 展示 L1 提示：只让用户说最确定的一点。
7. 输入“和双方确认有关”，点击“我想起来了”。
8. 展示强制重答提示。
9. 输入完整回答。
10. 回答一道穿插题。
11. 进入 TCP 变式重测。
12. 输入无提示回答。

旁白重点：

> 用户卡住后，AI 不会直接给标准答案。L1 只做认知引导，L2 只能基于用户自己说过的内容追问，L3 才允许给一个最小知识线索。恢复以后，系统必须撤掉提示，让用户重新完整回答原题。接着不会马上重复原题，而是先穿插其他题，再换一种问法重测同一个知识点。

## 2:15 - 2:45 结果页说明

展示结果页：

> 结果页不展示虚假的综合面试分数，而是展示知识提取训练记录：哪些知识点首次能独立提取，哪些发生了 Recall Failure，训练后是否变成 L0。这个 `L2 -> L0` 比“面试能力 82 分”更能说明训练是否有效。

## 2:45 - 3:00 技术与稳定性

展示 README 或代码结构：

> 技术上我用 Python 标准库服务和原生前端，尽量减少部署风险。核心流程由状态机控制，DeepSeek 只负责生成题目、提示、变式题和轻量判断。没有 API key 或 API 超时时会自动降级到 mock 内容，保证公网 Demo 可以完整跑通。

## 录屏注意

- 前 30 秒直接展示 `L2 -> L0 ✓`。
- 不要花太久讲技术栈。
- 卡住时不要让 AI 给标准答案，重点展示“逼用户自己想”。
- 结尾强调：普通 ChatGPT 能给反馈，但不会稳定执行这套训练协议。
