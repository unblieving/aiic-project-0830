# AI 面试知识提取训练器 Product Memo

## 1. 目标用户与核心痛点

本项目面向第一次准备大厂技术实习面试、已有一定 CS 基础但缺乏正式面试经验的本科生。典型用户刷过 LeetCode，也复习过计网、操作系统、数据库等课程内容，但在真实面试的突然提问里，经常出现“我学过，但一下子说不出来”的状态。

这个痛点不是单纯的知识缺口，而是 Recall Failure：用户过去大量进行输入、理解、收藏、背诵，却缺少“突然被问 -> 主动提取 -> 组织表达 -> 被追问 -> 再次提取”的训练。因此，本项目没有把重点放在讲更多知识，而是训练用户把已有知识在压力提问下调出来。

## 2. 产品设计说明

一句话定位：让存量知识，答得出来。

产品核心闭环是 Progressive Recall Scaffolding + Surprise Retest：

```text
冷启动问题 -> 独立回答 -> 卡住 -> L1/L2/L3 渐进提示
-> 撤掉提示完整重答 -> 穿插其他题 -> 变式重测 -> 验证提示依赖变化
```

关键取舍：

- 做文字版，不做语音/视频，因为文字已经能验证核心训练机制。
- 做后端开发方向优先，不追求覆盖所有岗位。
- 做 1-3 个知识领域，不做大而全题库。
- 不给综合面试分数，只展示 `Lx -> Ly`，让用户看到提示依赖是否下降。
- 不做登录、历史 Dashboard、排行榜、社交分享、虚拟人等外围功能。

相比普通 ChatGPT，本产品的差异不是“也能问面试题”，而是程序强制执行训练协议：用户卡住后不会立刻收到标准答案，而是先被引导自己检索；恢复后必须撤掉提示重新回答；随后还会被换一种问法重测，最终用 `L2 -> L0` 这类结果展示训练是否真正有效。

## 3. 版本迭代记录

最初方案容易变成普通 AI 模拟面试：AI 提问、用户回答、AI 反馈和给答案。这个形态的问题是用户仍然在被动接受解释，无法证明下次面试是否真的能调出知识。

开发中将核心业务收敛为状态机，而不是让 LLM 自由决定流程。程序负责控制状态：`QUESTION`、`SCAFFOLD_L1`、`SCAFFOLD_L2`、`SCAFFOLD_L3`、`REANSWER`、`RETEST`、`RESULT`。LLM 只负责生成问题、提示、变式题和轻量判断。这样即使 API 异常，也能降级到 mock 内容，保证 Demo 不崩。

代码审阅后修复了几个风险：重测不能无条件算 L0；多个待重测知识点不能提前进入结果页；单领域训练也必须有穿插题；结果页不能把 LLM 文本直接注入 HTML。

## 4. 下一步设计

如果再给一周，优先做三件事：

- 引入更可靠的回答质量判断，让 L0/Failure 不只依赖轻量规则。
- 扩充题库和岗位覆盖，但仍保持“做深做窄”的训练机制。
- 增加训练后的错题本和间隔复测，让 Surprise Retest 从单次 Demo 扩展为长期训练。

仍然不会优先做综合分、排行榜、虚拟人、视频面试等功能，因为它们会稀释当前最重要的产品假设。

## 5. AI 工具使用

- Codex：用于阅读 Spec、拆解实现计划、编写代码、测试、代码审阅修复、README/HANDOFF 文档。
- DeepSeek API：作为产品运行时的 LLM 接口，用于生成问题、渐进提示、变式重测和回答判断。
- Mock fallback：用于 API key 缺失、超时或异常时保持演示闭环稳定。

## 6. 当前实现状态

当前 MVP 已完成：

- Setup：岗位、1-3 个领域、自评。
- Training：文字回答、我卡住了、L1/L2/L3、我想起来了、强制完整重答。
- Verification：穿插题、变式重测、首次 vs 重测对比。
- Result：Independent Recall、Improved Recall、Knowledge Gap、知识点、自评、First Recall Level、Retest Recall Level、`Lx -> Ly`。
- Optional Voice：接入火山引擎豆包语音 TTS 与一句话 ASR，作为演示增强；文本模式仍是最稳定的 MVP 主路径。
- Submission materials：README、Demo Script、调研记录、原型说明、架构图、测试与性能记录。

验证命令：

```powershell
python -m unittest discover -s tests
node --check static\app.js
```

当前本地测试覆盖 57 个用例。
