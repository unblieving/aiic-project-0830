# AI 面试知识提取训练器 Product Memo

> **一句话定位：让存量知识，答得出来。**

## 1. 目标用户与核心痛点

### 我们不是在解决"没学过"，而是在解决"学过，却在关键时刻拿不出来"

本项目面向**第一次准备技术实习面试、已有一定 CS
基础但缺乏正式面试经验的本科生**。他们往往刷过
LeetCode，也学过计算机网络、操作系统、数据库等课程，甚至在复习时觉得"这个我会"。但一旦进入真实面试，被突然问到一个知识点，常常会出现一种很典型的状态：

**"我明明学过，但现在就是一下子说不出来。"**

最初我也很容易把这个问题理解成"知识掌握得不够"，但继续拆解之后，我发现这里其实混杂着两类完全不同的问题：

-   **Knowledge Gap**：这个知识本身就没有真正掌握；
-   **Recall
    Failure**：知识曾经学过、理解过，但在无提示、突然提问的环境下无法主动提取出来。

我认为后者被很多面试产品低估了。

大量学习产品和 AI
面试产品都在优化"输入"：给更多知识、更多题目、更完整的标准答案、更细致的点评。但真实面试考察的不是"看到答案后能不能理解"，而是：

> **在没有任何提示的情况下，能不能把已经学过的知识主动调出来，并组织成一个可表达的答案。**

这带来了一个更值得警惕的错觉：

**"看懂答案"不等于"下次能独立说出来"。**

如果用户卡住以后，AI
立刻给出标准答案，用户通常很快就会产生"原来是这个，我其实知道"的熟悉感。但这种熟悉感并不能证明下一次突然被问时，他真的能够独立提取。

因此，我没有把产品重点放在"AI
能不能给出更好的答案"，而是放在一个更窄、但我认为更真实的问题上：

> **如何训练用户把已经存在脑中的知识，在面试压力和无提示条件下真正调出来？**

------------------------------------------------------------------------

## 2. 产品设计说明

### 核心产品假设

如果真正的问题是 Recall
Failure，那么产品就不应该在用户卡住的第一时间把答案交给他。

因为**标准答案虽然最快解决了当前这道题，却可能直接绕过了我们真正想训练的能力------主动提取。**

因此，产品的核心不是"问答 + 批改"，而是一套强制执行的训练协议：

``` text
冷启动问题
→ 独立回忆
→ 发生 Recall Failure
→ L1 / L2 / L3 渐进式提示
→ 撤掉提示，完整重答
→ 穿插其他知识点
→ Surprise Retest
→ 验证撤掉帮助后是否仍能独立调出
```

### Progressive Recall Scaffolding：提示不是答案，而是逐步减少的"扶手"

当用户卡住时，系统不会立刻给标准答案，而是逐级提供帮助：

-   **L1：认知/元认知方向提示**------只帮助用户寻找回忆路径，不提供新知识；
-   **L2：利用用户自己之前说过的内容**------从用户已有表达中寻找线索，仍尽量不注入新知识；
-   **L3：最小知识提示**------只提供恢复回忆所必需的最小新信息，而不是完整答案。

一旦用户表示"想起来了"，提示就被撤掉，用户必须重新完整回答。

这里有一个刻意的设计：**经过提示 ≠ 成功调出。**

只有用户在重新回答后，答案本身被判断为基本正确，才会被记录为
`Recovered`；如果经过 L3 后仍然无法正确回答，则进入
`Knowledge Gap`，并在最终复盘中给出参考答案。

### Surprise Retest：提示后答对，还不足以证明真的会了

做到渐进提示以后，我又意识到一个问题：

**如果用户刚刚看完提示就答对，我们仍然无法区分"真正恢复了知识"与"只是暂时依赖刚才的提示"。**

所以产品没有在"提示后答对"这里结束，而是继续：

1.  撤掉提示；
2.  插入其他知识点形成干扰；
3.  之后换一种问法再次测试同一知识；
4.  比较首次提取与重测时的提示依赖。

这也是为什么结果页不强调一个笼统的"面试得分"，而更关注：

``` text
L2 → L0
L3 → L1
Knowledge Gap
```

我真正想让用户看到的不是"这次得了 78 分"，而是：

> **刚才必须在第二层提示下才能想起来的知识，现在能不能不靠提示自己调出来？**

### AI 应该生成内容，但不应该决定产品逻辑

实现过程中还有一个重要取舍：**不让 LLM 自由控制整个面试流程。**

LLM
擅长的是生成多样化问题、根据上下文生成个性化提示、生成变式题以及进行语义判断；但如果把"什么时候提示、什么时候重答、什么时候重测"也完全交给
LLM，核心训练协议就会被模型随机性破坏。

因此最终采用：

**程序负责训练协议，LLM 负责内容生成。**

程序控制：

``` text
QUESTION
→ SCAFFOLD_L1
→ SCAFFOLD_L2
→ SCAFFOLD_L3
→ REANSWER
→ RETEST
→ RESULT
```

DeepSeek 则负责动态生成问题、针对当前回答生成提示、生成 Surprise Retest
变式题、判断回答质量，以及为最终的 Knowledge Gap 生成简洁参考答案。

这使 AI 成为产品机制中的"生成器和判断器"，而不是产品本身。

### 我刻意没有做什么

16 小时里最重要的不是尽可能多地完成 Feature，而是判断：

> **什么功能能够证明核心产品假设成立？**

因此我主动放弃了很多看起来"像 AI 面试产品"的功能：

**没有做综合面试分数。**\
一个 78 分并不能告诉用户刚才卡住的知识现在是否能够调出；`L2 → L0`
对这个产品更有意义。

**没有把大而全题库作为核心。**\
产品首先聚焦技术实习面试和少数知识领域，因为我要验证的是 Recall Training
机制，而不是题库规模。

**没有做登录、排行榜、社交分享、复杂 Dashboard、虚拟人。**\
这些功能可以提高产品完整度，但不能回答"渐进提示 + 撤除提示 +
重测是否真的改善知识提取"这个核心问题。

**语音也没有被定义为 MVP 成立的前提。**\
我先用文字模式验证完整 Recall Loop；核心机制成立后，再接入 TTS /
ASR，让交互更接近真实面试。这样即使语音增强层出现波动，核心训练价值仍然可以独立成立。

------------------------------------------------------------------------

## 3. 版本迭代记录

### V0：普通 AI 模拟面试

最初的方案很自然：

``` text
AI 提问
→ 用户回答
→ AI 判断
→ 给反馈 / 标准答案
```

但很快我意识到一个问题：

**这件事 ChatGPT 本身已经能做。**

更重要的是，这个流程仍然在强化"看解释"，而不是训练"主动提取"。用户这一次看懂了，并不能证明下一次面试时能够说出来。

所以我放弃了把"回答点评得更详细"作为主要方向。

### V1：用户卡住后，不立刻给答案

第一步变化是把"卡住"本身变成一个产品状态。

用户说"不知道""忘了""想不起来"时，不直接判错，也不马上展示答案，而是尝试帮助他自己恢复。

这一步把产品从"判断你会不会"，转向了"训练你把知识调出来"。

### V2：从一次提示，变成 Progressive Recall Scaffolding

随后我发现，"给一个提示"仍然太粗糙。

提示过强，相当于变相给答案；提示太弱，又没有帮助。

因此将提示拆成 L1 / L2 / L3，让系统尽可能晚地注入新知识：

``` text
先帮助寻找回忆路径
→ 再利用用户自己的已有内容
→ 最后才给最小知识线索
```

这里的原则是：**能让用户自己想起来，就不要替用户想。**

### V3：提示后答出来，并不等于训练成功

这是过程中最关键的一次修正。

早期实现很容易把"经过 Scaffold 后重新提交"直接视为
`Recovered`。但测试时发现，即使用户最后随便输入内容，也可能被错误记录为"成功调出"。

这暴露了一个产品逻辑问题：

> **提示过程是否完成，与答案是否真的正确，是两件不同的事。**

因此最终把 `Recovered` 的条件收紧为：

``` text
使用过 Scaffold
+
重新独立回答
+
最终答案本身被判断为正确
=
Recovered
```

否则继续提示；L3 后仍无法正确回答，则记录为 `Knowledge Gap`。

### V4：从"恢复"继续追问到"能否脱离提示"

即使修正了
`Recovered`，仍有一个问题：刚刚得到提示后答对，可能只是短时记住了提示。

于是加入 **Surprise Retest**：

``` text
恢复
→ 插入其他题形成干扰
→ 换一种问法
→ 再次无提示提取
```

到这里，产品才形成我认为真正完整的闭环：

> **发现提取失败 → 最小帮助 → 独立恢复 → 延迟验证。**

### 工程实现上的取舍

为了保证这套训练协议不会被 LLM
随机性打乱，最终将核心业务收敛成状态机。LLM
不负责决定流程，只在指定状态下完成生成与判断。

同时保留 fallback，使 API 异常时仍能维持基本演示闭环。

这个实现选择并不是为了把架构做复杂，而是为了确保：**产品最重要的行为是确定的，AI
最有价值的内容是动态的。**

------------------------------------------------------------------------

## 4. 下一步设计

如果再给我一周，我不会首先增加更多"看起来完整"的外围功能，而会继续围绕同一个问题向深处做。

### 1. 让 Recall 判断更可靠

目前 LLM 已参与回答质量判断，但 Recall Failure、Partial Knowledge
与真正的 Knowledge Gap 之间仍有进一步校准空间。

下一步会建立更系统的判定标准和测试集，重点减少两类错误：

-   "随便说了相关关键词"却被判定为成功；
-   用户其实知道，只是表达不完整，却被过早判成 Knowledge Gap。

### 2. 把 Surprise Retest 从一次 Session 延伸到长期记忆

现在的 Surprise Retest 主要发生在单次训练内。

如果有一周，我会把它扩展到：

``` text
本次训练
→ 次日
→ 3 天后
→ 7 天后
```

真正观察一个知识点是否从"依赖提示"逐渐变成"稳定独立提取"。

这样产品关注的指标也会从"做了多少题"进一步转向：

> **多少知识真正从提示依赖变成了稳定 Recall。**

### 3. 扩展岗位和知识覆盖，但不牺牲训练深度

会逐步扩充后端、算法等技术岗位的知识覆盖，同时保持每轮只聚焦少量知识点。

我不会因为题库变大，就把产品重新变成"无限刷题"。

因为这个产品的核心竞争力不是题更多，而是：

**每一个卡住的知识点，都被真正训练到能够再次提取。**

仍然不会优先投入排行榜、综合评分、虚拟人等功能。它们可能提高表面完成度，但不是当前产品假设最需要验证的部分。

------------------------------------------------------------------------

## 5. AI 工具使用

### Codex

Codex 主要用于工程实现过程，包括：

-   阅读和落实 Spec；
-   拆解开发计划；
-   编写和修改代码；
-   单元测试与代码审阅；
-   定位状态机和前后端流程 Bug；
-   README、HANDOFF 等工程文档维护。

在开发过程中，我没有把产品决策完全交给 Coding
Agent，而是先确定产品假设和关键取舍，再让 Agent 在这些约束内实现和修复。

### DeepSeek API

DeepSeek 是产品运行时的核心 LLM，主要用于：

-   根据岗位、领域和已有知识点动态生成面试问题；
-   根据用户当前回答动态生成 L1 / L2 / L3 提示；
-   判断答案属于 Correct / Partial / Incorrect / Recall Failure；
-   生成 Surprise Retest 的变式问题；
-   为最终 Knowledge Gap 生成简洁的参考答案。

核心原则仍然是：

> **让 LLM
> 负责它擅长的生成与语义判断，让程序负责必须稳定执行的训练协议。**

### 其他 AI 辅助

AI 也参与了需求讨论、产品逻辑反思、边界情况分析和 Memo
整理。但最终保留哪些功能、砍掉哪些功能，以及产品究竟解决什么问题，始终围绕同一个核心假设进行判断：

**用户真正缺的，也许不是再学一次，而是练习在没有帮助时把已经学过的东西拿出来。**

------------------------------------------------------------------------

## 6. 当前 MVP

当前 MVP 已形成完整的文字训练闭环：

``` text
Setup
岗位 / 领域 / 自评
        ↓
Dynamic Question
        ↓
Independent Recall
        ↓
Recall Failure?
   ↙            ↘
  否              是
  ↓               ↓
下一知识点      L1 → L2 → L3
                  ↓
               完整重答
                  ↓
             Answer Judge
                  ↓
        Recovered / Knowledge Gap
                  ↓
             Surprise Retest
                  ↓
                Result
```

结果页不以综合分数作为最终反馈，而是区分：

-   **Independent Recall**：无需提示即可正确回答；
-   **Recovered**：经过若干层提示后，最终重新独立回答正确；
-   **Knowledge Gap**：提示后仍未形成正确答案，并提供参考答案；
-   **Retest Result**：通过变式重测观察提示依赖是否下降。

对于 `Recovered`，结果页直接展示恢复路径，例如：

``` text
独立回忆 → L1 → L2 → 成功调出
```

让用户看到的不是一个抽象分数，而是一次具体的认知变化。

------------------------------------------------------------------------

## 结语：我想验证的不是"AI 能不能当面试官"

做完这个 MVP 后，我认为最值得验证的产品问题已经不是：

> AI 能不能生成一道不错的面试题？

这件事今天的大模型已经做得很好。

真正值得继续验证的是：

> **AI
> 能不能不急着把答案告诉用户，而是在正确的时机只提供最少的帮助，让用户自己把知识重新找回来，并证明下一次没有帮助时仍然能够做到？**

如果答案是可以，那么 AI
在学习产品中的角色就不只是一个更聪明的"答案提供者"。

它也可以成为一个**知道什么时候不该告诉你答案的训练者**。
